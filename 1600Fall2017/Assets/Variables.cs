using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Variables : MonoBehaviour {
    
    // This is a string, it shows text when called.
    string myHerro = "Hello World!";
    // or this:
    string myDrink = "I love Sprite."

    // This is a double, it shows large numbers when called.
    double MyDouble = 1 e 100;

    // This is a int or integer, it shows whole numbers when called.
    int myInt = 5;

    // This is a float, it shows numbers with decimals when called.
    float myFloat = 3.1

    // This is a string, it shows text when called. 

	    // Use this for initialization
	void Start ()
    {
        print(myString);
	}
	
	    // Update is called once per frame
	void Update ()
    {
	    	
	}
}
